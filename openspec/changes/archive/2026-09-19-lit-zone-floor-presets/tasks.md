## 1. Геометрическое ядро

- [x] 1.1 Написать красные тесты пола и `H`: при `H = 1200` пол на `y = -1200` и посадочная плоскость на `y = 0`; сцена отвергается при `H <= 0` и при `H <= lower.thickness`; глаз с `y < -H` садится на `y = -H` без сдвига X. Убедиться, что `npm test` падает
- [x] 1.2 Добавить `lower.heightFromFloor` в типы и дефолт 1200, валидацию, пол в `buildScene` и клип в `placeEye`; довести тесты 1.1 до зелёного без импорта React
- [x] 1.3 Написать красные тесты зоны: глаз внутри зоны ⇔ `hasDirectGlare`; точка на `y = 0` под `downward`-лентой внутри; точка в тени бленды снаружи; все точки зоны с `y >= -H` и `x >= 0`. Убедиться, что `npm test` падает
- [x] 1.4 Реализовать первый удар луча и зону как объединение вееров сэмплов в устойчивой рамке комнаты; довести тесты 1.3 до зелёного. Блик emit→глаз не менять

## 2. SVG-схема

- [x] 2.1 Расширить модель схемы полом и полигоном зоны; проверить тестом, что пол на `y = -H` и стена доходит до пола, а лента по-прежнему не точка
- [x] 2.2 В `SectionView` всегда рисовать пол; в режиме `lit` — заливку зоны без лучей к глазу и без `plantFan`; в режиме `glare` — лучи и веер как сейчас. Проверить разметкой (`data-view` / отсутствие лучей в `lit`) и в браузере, что глаз остаётся и тумблер переключает картинку

## 3. Контролы и URL

- [x] 3.1 Поле и слайдер `H` (дефолт 1200); смена `H` не меняет `viewer.eyeHeight`. Тестом: нет `lh` в query → 1200; `H = 18` при толщине 18 даёт валидацию и прежнюю схему
- [x] 3.2 Тумблер «лучи / заливка» у схемы; URL `view=glare|lit` (нет ключа или неизвестное → glare) и `lh`. Тестом: ссылка с `view=lit&lh=1400` открывает заливку и `H = 1400`; старая ссылка без ключей — лучи и 1200
- [x] 3.3 Кнопки пресетов «Стоя» / «Сидя» / «Лёжа» (1600 / 1200 / 600 от пола): `eyeHeight = пресет − H`, расстояние не трогать, затем обычная посадка. Тестом: при `H = 1200` стоя → 400, сидя → 0, лёжа → −600, `vd` тот же
- [x] 3.4 В `openspec/project.md` описать пол, `H`, зону, тумблер и пресеты 1600/1200/600; сверить с дельта-спеками

## 4. Статическая сборка

- [x] 4.1 Прогнать `npm test && npm run lint && npm run build` и убедиться, что `dist/` собирается без API и без правок Docker
