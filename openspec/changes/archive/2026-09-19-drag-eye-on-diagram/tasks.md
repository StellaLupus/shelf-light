## 1. Геометрическое ядро

- [x] 1.1 Написать красные тесты посадки глаза (внутренность нижней полки, `x < 0` → `x = 0` и Y тот же, залив без сдвига, точка на грани полки без сдвига, внутренность бленды, внутренность тела `ell`, точка внутри нарисованного `triangle`/`radius` без непрозрачного Rect не сдвигается) и убедиться, что `npm test` падает
- [x] 1.2 Реализовать чистую посадку в `src/geometry`, вызывать её из `evaluateScene` после сборки сцены и довести тесты 1.1 до зелёного без импорта React
- [x] 1.3 Добавить в `openspec/project.md` в раздел наблюдателя, что глаз на схеме можно схватить и что посадка не внутри окклюдеров и не за стеной, и сверить формулировку с дельта-спекой

## 2. SVG-схема

- [x] 2.1 Написать тест перевода экранных координат в мм сцены (обратная матрица + инверсия Y) и реализовать чистую функцию в `src/viz`; `npm test` зелёный
- [x] 2.2 Добавить на маркер глаза увеличенный хит, курсоры `grab`/`grabbing`, pointer capture и заморозку `viewBox` на время жеста; проверить разметкой, что хит больше видимого кружка и не на всём SVG, и в браузере — что кружок тащится, пустой клик не двигает глаз, масштаб во время жеста не прыгает, статус блика следует за точкой

## 3. Контролы и URL

- [x] 3.1 В `App` после успешного расчёта записать посаженный `scene.eye` в `viewer` и проверить тестом, что при глазе внутри полки поля `vd`/`vh` после apply совпадают с посаженной точкой, а не с исходной внутренностью
- [x] 3.2 Проверить, что `parseParams` URL с глазом внутри верхней полки после `evaluateScene` и сериализации даёт query без внутренности, и что ползунки/поля по-прежнему двигают тот же `viewer`

## 4. Статическая сборка

- [x] 4.1 Прогнать `npm test && npm run lint && npm run build` и убедиться, что `dist/` собирается без API и без правок Docker
