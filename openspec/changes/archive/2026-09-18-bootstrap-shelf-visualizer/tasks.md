## 1. Геометрическое ядро

- [x] 1.1 Поднять Vitest и написать красные тесты сцены (origin, мм), отрезка ленты `corner`/`downward`, брака `width <= 0` и выноса за кромку; убедиться, что `npm test` падает на отсутствии реализации
- [x] 1.2 Добавить красные тесты блика: окклюзия блендой и более глубокой нижней полкой, блик от внутреннего края, отсутствие блика когда все сэмплы закрыты, согласованность `hasDirectGlare` и `samples[]`
- [x] 1.3 Реализовать чистые функции в `src/geometry` (сборка сцены, отрезок ленты, клип по прямоугольникам, ≥16 внутренних точек + концы) и довести `npm test` до зелёного без импорта React

## 2. SVG-схема

- [x] 2.1 Нарисовать разрез (стена, полки, бленда, лента полосой, глаз, веер вниз) из результата ядра и проверить, что лента не точка
- [x] 2.2 Нарисовать лучи сэмплов (красный открыт, зелёный перекрыт) и текстовый статус, совпадающий с `hasDirectGlare`

## 3. Контролы и URL

- [x] 3.1 Связать каждый числовой параметр со слайдером и полем; проверить, что движение слайдера меняет поле и схему, а ввод в поле двигает слайдер
- [x] 3.2 Сериализовать состояние в query string и проверить, что reload и открытие той же ссылки восстанавливают тип монтажа, размеры и статус блика
- [x] 3.3 Показать ошибку валидации при невозможной сцене (зазор ≤ 0, лента за кромкой) без пустого экрана; на узкой ширине схема над контролами

## 4. Статическая сборка и Docker

- [x] 4.1 Настроить Vite-сборку в `dist/` и проверить `npm run build` + `npm run preview` без API
- [x] 4.2 Добавить multi-stage Dockerfile (nginx:alpine) и compose; проверить, что контейнер отдаёт приложение на опубликованном порту без backend-сервиса
