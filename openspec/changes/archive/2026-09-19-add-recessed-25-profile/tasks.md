## 1. Геометрическое ядро

- [x] 1.1 Написать красные тесты SKU `recessed25`: тело 40 мм от `offsetFromWall`, паз 35×14 между упорами 2,5 мм, молоко 14 мм под 25° от вертикали, окно 25 мм от нижнего края до конца выреза, `led.width` не двигает молоко — и убедиться, что `npm test` падает на четырёх старых монтажах
- [x] 1.2 Написать красные тесты выреза и разворота: кап и две щеки в окклюдерах, паз не окклюдер, сэмпл молока в воздухе полости, щека закрывает луч сквозь плиту, `facing=wall` / `room` зеркалит начинку при том же span 40 мм, глаз в воздухе паза остаётся, глаз в щеке выталкивается
- [x] 1.3 Написать красные тесты валидации: толщина полки < 14 отвергается, `offset + 40 > depth` отвергается, `led.width = 0` при `recessed25` сцену не бракует; `downward` / угловые у бленды по ширине ведут себя как раньше
- [x] 1.4 Реализовать `recessed25`, `facing`, константы SKU и вырез AABB в `src/geometry` (типы, сцена, валидация) и довести тесты 1.1–1.3 до зелёного без импорта React
- [x] 1.5 Обновить раздел монтажа ленты в `openspec/project.md` под врезной SKU и вырез и проверить, что текст совпадает с дельта-спекой

## 2. SVG-схема

- [x] 2.1 Расширить `buildDiagram` вырезом паза и сечением канала `recessed25` и проверить тестом, что верхняя полка не рисуется сплошным прямоугольником без дырки, а молоко не точка
- [x] 2.2 Нарисовать вырез и канал в `SectionView` и проверить в браузере оба разворота: паз читается, лучи красный/зелёный по флагу ядра, заливка зоны совпадает со статусом блика

## 3. Контролы и URL

- [x] 3.1 Пятое радио «Врезной 25°», тумблер «к стене / в комнату» только на нём; скрыть ширину и свес, показать смещение; проверить, что на `triangle` разворота нет, на `downward` нет разворота и есть ширина, на `recessed25` нет ширины и свеса
- [x] 3.2 `parseParams` / `serializeParams`: писать `mount=recessed25` и `lface=wall|room`, читать отсутствие `lface` как `wall`, `corner` как `triangle`; проверить тестом round-trip и `?mount=recessed25` без `lface`

## 4. Статическая сборка

- [x] 4.1 Прогнать `npm test && npm run lint && npm run build` и убедиться, что `dist/` собирается без API и без регрессии Docker-образа (хостинг не менять)
