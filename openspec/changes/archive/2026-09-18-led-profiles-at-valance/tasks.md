## 1. Геометрическое ядро

- [x] 1.1 Написать красные тесты кармана и трёх профилей (стык с блендой, кромка при нулевой бленде, отказ если W не помещается, дуга r=W, квадрат `ell` с двумя гранями, катеты W и гипотенуза W√2 у `triangle`) и убедиться, что `npm test` падает на старом `corner` у стены
- [x] 1.2 Написать красные тесты блика: луч сквозь тело `ell` закрыт; блик есть, если открыта хотя бы одна из двух граней; дуга даёт ≥16 внутренних сэмплов; `downward` и `profileDrop` ведут себя как раньше
- [x] 1.3 Заменить `corner`/`emitAngle` на `radius`/`ell`/`triangle`, список `emitSurfaces` и опциональный `profileBody` в `src/geometry`; довести тесты 1.1–1.2 до зелёного без импорта React
- [x] 1.4 Обновить раздел монтажа ленты в `openspec/project.md` под карман у бленды и три профиля и проверить, что текст совпадает с дельта-спекой

## 2. SVG-схема

- [x] 2.1 Расширить `buildDiagram` телом профиля (четверть круга / квадрат / треугольник / полоса `downward`) и проверить тестом, что `triangle` не рисуется отрезком у стены
- [x] 2.2 Нарисовать тело в `SectionView` и проверить в браузере, что три угловых монтажа визуально различимы и лучи по-прежнему красный/зелёный по флагу ядра

## 3. Контролы и URL

- [x] 3.1 Четыре радио (вниз / радиусный / Г-образный / треугольный), скрыть угол всегда и смещение кроме `downward`; проверить, что на `triangle` поля угла и смещения нет, на `downward` смещение есть
- [x] 3.2 `parseParams`/`serializeParams`: писать `radius`/`ell`/`triangle`/`downward`, читать `corner` как `triangle`, игнорировать `lea`; проверить тестом round-trip и сценарий `?mount=corner`

## 4. Статическая сборка

- [x] 4.1 Прогнать `npm test && npm run lint && npm run build` и убедиться, что `dist/` собирается без API и без регрессии Docker-образа (хостинг не менять)
